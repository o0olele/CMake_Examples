# Usages
- Install OpenCV in custom path.

~~~shell
# your opencv cmake parameters may like:
cmake \
-D CMAKE_INSTALL_PREFIX=/path/to/libcv \
# ...
~~~

- cmake

~~~shell
cmake .
make
~~~

# Some descriptions

`find_package`: Finds and loads settings from an external project.
